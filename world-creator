#!/usr/bin/env bash
# Baseline launcher (no GPU denoiser) for World Creator 2026.4 — thin wrapper over ./wc.
# For another version use ./wc "World Creator <ver>" --no-denoise directly.
HERE=$(cd "$(dirname "$(readlink -f "$0")")" && pwd)
exec "$HERE/wc" "World Creator 2026.4" --no-denoise "$@"
